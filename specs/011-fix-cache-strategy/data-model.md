# Data Model: キャッシュ戦略修正

**Branch**: `011-fix-cache-strategy` | **Date**: 2026-05-28

## スキーマ変更

**なし**。本フィーチャーはキャッシュ層とレスポンスヘッダーの変更のみであり、データベーススキーマの変更は一切発生しない。

## キャッシュエントリ構造（論理モデル）

`unstable_cache` によって管理される論理的なキャッシュエントリ:

| キャッシュキー | タグ | TTL | 用途 |
|--------------|------|-----|------|
| `point-balance-{userId}` | `user-points-{userId}` | 永続 (`revalidate: false`) | ユーザー別ポイント残高 |

### 無効化トリガー

| イベント | 呼び出し元 | 無効化対象 |
|---------|-----------|----------|
| POST `/api/posts`（投稿作成・ポイント消費） | Route Handler | `user-points-{userId}` |

## 既存エンティティへの影響

変更なし。`user_point` テーブルのスキーマおよびデータアクセスロジックは変更しない。
